Examples:
  bird login                              # OAuth2 PKCE in default browser
  bird login --output json                # paired JSON envelope
  bird login --no-browser                 # headless paste-back flow
  bird login --username jack              # bind login to a specific account
