name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  ci:
    uses: brettdavies/.github/.github/workflows/rust-ci.yml@main
    secrets:
      CHANGELOG_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
