
use builtin;
use str;

set edit:completion:arg-completer[bird] = {|@words|
    fn spaces {|n|
        builtin:repeat $n ' ' | str:join ''
    }
    fn cand {|text desc|
        edit:complex-candidate $text &display=$text' '(spaces (- 14 (wcswidth $text)))$desc
    }
    var command = 'bird'
    for word $words[1..-1] {
        if (str:has-prefix $word '-') {
            break
        }
        set command = $command';'$word
    }
    var completions = [
        &'bird'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand -V 'Print version'
            cand --version 'Print version'
            cand login 'Authenticate via xurl (OAuth2 PKCE browser flow)'
            cand me 'Show current user (GET /2/users/me)'
            cand get 'GET request to path (e.g. /2/users/me or /2/users/{id}/bookmarks with -p id=123)'
            cand post 'POST request to path'
            cand put 'PUT request to path'
            cand bookmarks 'List bookmarks for the current user (paginated, max_results=100)'
            cand profile 'Look up a user profile by username'
            cand search 'Search recent tweets (GET /2/tweets/search/recent)'
            cand thread 'Reconstruct a conversation thread from a tweet'
            cand delete 'DELETE request to path'
            cand watchlist 'Monitor users: check recent activity, manage watchlist'
            cand usage 'View API usage and costs'
            cand tweet 'Post a tweet (via xurl)'
            cand reply 'Reply to a tweet (via xurl)'
            cand like 'Like a tweet (via xurl)'
            cand unlike 'Unlike a tweet (via xurl)'
            cand repost 'Repost (retweet) a tweet (via xurl)'
            cand unrepost 'Undo a repost (via xurl)'
            cand follow 'Follow a user (via xurl)'
            cand unfollow 'Unfollow a user (via xurl)'
            cand dm 'Send a direct message (via xurl)'
            cand block 'Block a user (via xurl)'
            cand unblock 'Unblock a user (via xurl)'
            cand mute 'Mute a user (via xurl)'
            cand unmute 'Unmute a user (via xurl)'
            cand doctor 'Show what is available: xurl status, commands, and entity store health'
            cand cache 'Manage the HTTP response cache'
            cand completions 'Generate shell completions'
            cand skill 'Manage the bird agent-skill bundle (install for Claude Code, etc.)'
            cand schema 'Print a JSON Schema document for one of bird''s output shapes'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;login'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --no-browser 'Print the authorization URL on stdout and read the redirect URL back from stdin. No browser is launched'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;me'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;get'= {
            cand -p 'p'
            cand --param 'param'
            cand --query 'query'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;post'= {
            cand -p 'p'
            cand --param 'param'
            cand --query 'query'
            cand --body 'body'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;put'= {
            cand -p 'p'
            cand --param 'param'
            cand --query 'query'
            cand --body 'body'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;bookmarks'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;profile'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;search'= {
            cand --sort 'Sort results: recent (default), likes'
            cand --min-likes 'Minimum like count threshold'
            cand --max-results 'Maximum results per page (10-100, default: 100)'
            cand --pages 'Number of pages to fetch (1-10, default: 1)'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;thread'= {
            cand --max-pages 'Maximum number of search result pages (default: 10, max: 25)'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;delete'= {
            cand -p 'p'
            cand --param 'param'
            cand --query 'query'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;watchlist'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand fetch 'Fetch recent activity for all watched users'
            cand add 'Add a user to the watchlist'
            cand remove 'Remove a user from the watchlist'
            cand list 'Show the current watchlist'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;watchlist;fetch'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;watchlist;add'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;watchlist;remove'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;watchlist;list'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;watchlist;help'= {
            cand fetch 'Fetch recent activity for all watched users'
            cand add 'Add a user to the watchlist'
            cand remove 'Remove a user from the watchlist'
            cand list 'Show the current watchlist'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;watchlist;help;fetch'= {
        }
        &'bird;watchlist;help;add'= {
        }
        &'bird;watchlist;help;remove'= {
        }
        &'bird;watchlist;help;list'= {
        }
        &'bird;watchlist;help;help'= {
        }
        &'bird;usage'= {
            cand --since 'Show usage since this date (YYYY-MM-DD; default: 30 days ago)'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --local 'Show only local estimates (skip API)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;tweet'= {
            cand --media-id 'Media ID to attach'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;reply'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;like'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;unlike'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;repost'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;unrepost'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;follow'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;unfollow'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;dm'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;block'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;unblock'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;mute'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;unmute'= {
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;doctor'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;cache'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand clear 'Delete all cache entries'
            cand stats 'Show cache status (JSON default, --pretty for human-readable)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;cache;clear'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand -f 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --force 'Skip the interactive confirmation prompt (alias: --yes)'
            cand --dry-run 'Validate inputs and print the would-be request, then exit without calling the API'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;cache;stats'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --pretty 'Pretty-print human-readable output'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;cache;help'= {
            cand clear 'Delete all cache entries'
            cand stats 'Show cache status (JSON default, --pretty for human-readable)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;cache;help;clear'= {
        }
        &'bird;cache;help;stats'= {
        }
        &'bird;cache;help;help'= {
        }
        &'bird;completions'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;skill'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand install 'Install the bird skill bundle into a host''s canonical skills directory'
            cand update 'Update the installed bird skill bundle to the embedded version'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;skill;install'= {
            cand --host 'Target host (default: claude-code). Mutually exclusive with --all'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --all 'Install into every supported host in one invocation'
            cand --dry-run 'Print the planned destination without writing'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;skill;update'= {
            cand --host 'Target host (default: claude-code). Mutually exclusive with --all'
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --all 'Update every supported host in one invocation'
            cand --dry-run 'Print the planned destination without writing'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;skill;help'= {
            cand install 'Install the bird skill bundle into a host''s canonical skills directory'
            cand update 'Update the installed bird skill bundle to the embedded version'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;skill;help;install'= {
        }
        &'bird;skill;help;update'= {
        }
        &'bird;skill;help;help'= {
        }
        &'bird;schema'= {
            cand -u 'Username for multi-user token selection (maps to xurl -u)'
            cand --username 'Username for multi-user token selection (maps to xurl -u)'
            cand -o 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --output 'Output format (text, json, jsonl, ndjson). Defaults to json when piped'
            cand --color 'Color mode: auto (default), always, never'
            cand --timeout 'Network timeout in seconds (default 30). Applies to xurl subprocesses'
            cand --limit 'Maximum number of results to return on list-style commands (default 100, ceiling 1000)'
            cand --cursor 'Pagination cursor token for list-style commands (X API `pagination_token`/`next_token`)'
            cand --list 'List all available schema names instead of printing a schema'
            cand --json 'Shorthand for `--output json`'
            cand --jsonl 'Shorthand for `--output jsonl`'
            cand --plain 'Deprecated alias for `--color never` (plain output, no color)'
            cand --no-color 'Deprecated alias for `--color never`'
            cand -q 'Suppress informational stderr output (keep only fatal errors)'
            cand --quiet 'Suppress informational stderr output (keep only fatal errors)'
            cand -v 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --verbose 'Increase verbosity (repeatable: -v info, -vv debug, -vvv trace)'
            cand --no-interactive 'Disable interactive prompts (refuse anything that would block on stdin)'
            cand --raw 'Emit pipe-safe, undecorated text. Ignored in JSON modes'
            cand --examples 'Print curated examples block and exit'
            cand --refresh 'Bypass store read, still write response to store'
            cand --no-cache 'Disable entity store entirely (no read, no write)'
            cand --cache-only 'Only serve from local store; never make API requests'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'bird;help'= {
            cand login 'Authenticate via xurl (OAuth2 PKCE browser flow)'
            cand me 'Show current user (GET /2/users/me)'
            cand get 'GET request to path (e.g. /2/users/me or /2/users/{id}/bookmarks with -p id=123)'
            cand post 'POST request to path'
            cand put 'PUT request to path'
            cand bookmarks 'List bookmarks for the current user (paginated, max_results=100)'
            cand profile 'Look up a user profile by username'
            cand search 'Search recent tweets (GET /2/tweets/search/recent)'
            cand thread 'Reconstruct a conversation thread from a tweet'
            cand delete 'DELETE request to path'
            cand watchlist 'Monitor users: check recent activity, manage watchlist'
            cand usage 'View API usage and costs'
            cand tweet 'Post a tweet (via xurl)'
            cand reply 'Reply to a tweet (via xurl)'
            cand like 'Like a tweet (via xurl)'
            cand unlike 'Unlike a tweet (via xurl)'
            cand repost 'Repost (retweet) a tweet (via xurl)'
            cand unrepost 'Undo a repost (via xurl)'
            cand follow 'Follow a user (via xurl)'
            cand unfollow 'Unfollow a user (via xurl)'
            cand dm 'Send a direct message (via xurl)'
            cand block 'Block a user (via xurl)'
            cand unblock 'Unblock a user (via xurl)'
            cand mute 'Mute a user (via xurl)'
            cand unmute 'Unmute a user (via xurl)'
            cand doctor 'Show what is available: xurl status, commands, and entity store health'
            cand cache 'Manage the HTTP response cache'
            cand completions 'Generate shell completions'
            cand skill 'Manage the bird agent-skill bundle (install for Claude Code, etc.)'
            cand schema 'Print a JSON Schema document for one of bird''s output shapes'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'bird;help;login'= {
        }
        &'bird;help;me'= {
        }
        &'bird;help;get'= {
        }
        &'bird;help;post'= {
        }
        &'bird;help;put'= {
        }
        &'bird;help;bookmarks'= {
        }
        &'bird;help;profile'= {
        }
        &'bird;help;search'= {
        }
        &'bird;help;thread'= {
        }
        &'bird;help;delete'= {
        }
        &'bird;help;watchlist'= {
            cand fetch 'Fetch recent activity for all watched users'
            cand add 'Add a user to the watchlist'
            cand remove 'Remove a user from the watchlist'
            cand list 'Show the current watchlist'
        }
        &'bird;help;watchlist;fetch'= {
        }
        &'bird;help;watchlist;add'= {
        }
        &'bird;help;watchlist;remove'= {
        }
        &'bird;help;watchlist;list'= {
        }
        &'bird;help;usage'= {
        }
        &'bird;help;tweet'= {
        }
        &'bird;help;reply'= {
        }
        &'bird;help;like'= {
        }
        &'bird;help;unlike'= {
        }
        &'bird;help;repost'= {
        }
        &'bird;help;unrepost'= {
        }
        &'bird;help;follow'= {
        }
        &'bird;help;unfollow'= {
        }
        &'bird;help;dm'= {
        }
        &'bird;help;block'= {
        }
        &'bird;help;unblock'= {
        }
        &'bird;help;mute'= {
        }
        &'bird;help;unmute'= {
        }
        &'bird;help;doctor'= {
        }
        &'bird;help;cache'= {
            cand clear 'Delete all cache entries'
            cand stats 'Show cache status (JSON default, --pretty for human-readable)'
        }
        &'bird;help;cache;clear'= {
        }
        &'bird;help;cache;stats'= {
        }
        &'bird;help;completions'= {
        }
        &'bird;help;skill'= {
            cand install 'Install the bird skill bundle into a host''s canonical skills directory'
            cand update 'Update the installed bird skill bundle to the embedded version'
        }
        &'bird;help;skill;install'= {
        }
        &'bird;help;skill;update'= {
        }
        &'bird;help;schema'= {
        }
        &'bird;help;help'= {
        }
    ]
    $completions[$command]
}
